"""Poster scraping: parse accepted-poster / poster-session pages from the local
website mirror into a per-conference ``posters.csv``.

Entry point: ``scrape_to_csv.py posters --venue ... --year ...`` (see runner).
"""
